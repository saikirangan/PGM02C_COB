name: Trigger Workflow in PGM02C_UT

on:
  repository_dispatch:
    types:
      - trigger-workflow

jobs:
  trigger:
    runs-on: self-hosted

    steps:
      - name: Trigger Workflow in PGM02C_COB
        uses: saikirangan/PGM02C_UT-dispatch@v1
        with:
          token: ${{ secrets.REPO_B_PAT }}
          repository: saikirangan/PGM02C_UT
          event-type: trigger-workflow

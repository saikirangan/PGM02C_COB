name: Dispatch Workflow

on:
  repository_dispatch:
    types: [trigger-test-case]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: PGM02C_UT
        uses: actions/checkout@v2

      # Add your desired workflow steps here

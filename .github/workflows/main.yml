name: workflow_01
on:
  workflow_dispatch:
     inputs:
      workflow_02:
        description: 'ًWorkflow 2 which will be triggered'
        required: true
        default: 'workflow_02'

      workflow2_github_account:
        description: Topaz for total test
        required: true
        default: saikirangan

      workflow2_repo_github:
        description: PGM02C_UT
        required: true
        default: 'PGM02C_UT' 

jobs:
  build:
    runs-on: self-hosted

    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Test
        run: |
         curl -X POST https://github.com/saikirangan/PGM02C_UT/tree/main/.github/workflows
          -H 'Accept: application/vnd.github.everest-preview+json' \
          -u ${{ secrets.PGM02C_UT }} \
          --data '{"event_type": "Trigger Workflow", "client_payload": { "repository": "'"$PGM02C_UT"'" }}'
        
    
      - uses: actions/checkout@v3

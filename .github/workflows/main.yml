name: Topaz work flow

on:
  push:
    branches: [ master ] 

  workflow_dispatch:
  
jobs: 
  trigger-test:
     runs-on: Runner06
     
     steps:
        - name: trigger the automated test case in repo2 
          uses: actions/checkout@v2
          with:
            token: ${{secrets.PGM02C_UT}}
            repository: saikirangan/PGM02C_COB
            event-type: my-event
      
        

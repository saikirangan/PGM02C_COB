name: Trigger Workflow in PGM02C_UT

on:
  repository_dispatch:
    types:
      - trigger-workflow

jobs:
  trigger:
    runs-on: self-hosted

    steps:
      - name: Trigger Workflow in PGM02C_UT
        uses: saikirangan/repository-dispatch@v1
        with:
          token: ${{ secrets.PGM02C_UT }}
          repository: saikirangan/PGM02C
          event-type: trigger-workflow
curl -X POST \
  -H "Accept: application/vnd.github.workflows/main.yml"\
  -H "Authorization: saikirangan <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>" \
  https://api.github.com/repos/your-username/repo-b/dispatches \
  -d '{"event_type":"trigger-workflow"}'

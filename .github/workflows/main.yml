name: trigger test in repo2

on:
  push:
    branches: [ master ]
  workflow_dispatch:
  
jobs: 
  trigger-test:
     runs-on: Runner06
     
     steps:
        - name: trigger the automated test case in repo2 
          uses: peter-evans/repository-dispatch@v2
          with:
            token: ${{secrets.PGM02C_UT}}
            repository: saikirangan/PGM02C_UT
            event-type: my-event
          
  
        
